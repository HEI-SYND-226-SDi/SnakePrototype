class Point {
    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
    }

    public x: number = 0;
    public y: number = 0;
}

enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT
}
